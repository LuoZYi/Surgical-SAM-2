# @package _group_

name: state
model:
  memory_prune_mode: state_aware
  memory_score_mode: cosine_motion_geometry
  num_frame_to_prune: 2
  protect_conditioning_memories: true
  use_recent_memory_guard: false
